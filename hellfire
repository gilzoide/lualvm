hell.help = [[
Welcome to lualvm hellfire build script!
========================================

Lua linker flags are found using `pkg-config` by default. To use a custom link
flags, set the `luaLib` variable

As with Lua, LLVM C++ flags are found using `llvm-config`. To use custom flags,
set the `llvmFlags` variable.
Likewise, LLVM linker flags are found using `llvm-config`. To use custom link
flags, set the `llvmLib` variable.

To build with debug symbols (-g compiler option), call with `Debug=1`]]

-- shadow build, please
hell.outdir = 'build'

-- our library name ^^
libName = 'lualvm.so'

-- source the real builds
feedHellFire 'src/hellfire'


-- run the test!
exclusiveTarget ('test', command {
	command = 'cd ' .. hell.outdir .. ' && lua test.lua',
})
